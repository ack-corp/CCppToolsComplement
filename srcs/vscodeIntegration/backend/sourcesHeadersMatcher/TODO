1 - order the declaration, normalize space, carriage return.
2 - manage them who have implemented function in headers

i suppose we should declare
each struct proto first
then
each typedef proto
then
their implementation

