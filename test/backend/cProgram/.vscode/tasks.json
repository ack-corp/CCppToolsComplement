{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "graph: build main (debug)",
      "type": "shell",
      "command": "make",
      "args": [
        "-f",
        "Makefile.main",
        "all"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": [
        "$gcc"
      ]
    }
  ]
}
